# ── Audio: music files are copyrighted — keep them local, never commit ──
*.mp3
*.wav
*.m4a
*.flac
*.aac
*.ogg
*.wma
*.aiff
*.opus

# Generated proposals (review-then-discard; the overview is the source of truth)
*.snapped.csv

# ── Rehearsal recorder ──
node_modules/
rehearsal-captures/

# ── Python (audio analysis tooling) ──
__pycache__/
*.pyc
.venv/
venv/

# ── Editor / OS noise ──
~$*.xlsx
~$*.docx
Thumbs.db
.DS_Store

# Rehearsal-clock script variants (derived; never load on the controller)
*.rehearsal.csv
