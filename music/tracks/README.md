# tracks/ (local only)

Purchased song MP3s live here — shared across all shows, since a bought
track is a permanent asset. Audio files are gitignored; log every track in
`../library.md` (artist, cut used, which show, where bought) so the
collection is rebuildable.
