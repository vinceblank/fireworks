# audio/ (local only)

Drop the purchased MP3s, SFX clips, and the final show mix here. Audio files
are gitignored (copyright) — only this README and any `*.analysis.json`
timing files get committed.

Rebuild instructions live in the show's music plan; track list in
`music/library.md`.
